import 'package:flutter/material.dart';

import 'cart.dart';

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return  Scaffold(
        appBar : CartAppBar(),
    // Replace Container with your actual body content
      );

  }
}
